import type { Equipment, OfficeFloor, Space } from '../types';

export const demoEquipment: Equipment[] = ['monitor', 'docking station', 'standing desk', 'whiteboard', 'projector', 'video conferencing', 'speakers', 'accessibility access'].map((name, index) => ({ id: index + 1, name }));
export const demoFloors: OfficeFloor[] = [{ id: 1, name: 'Atlas Floor', floor_number: 1, is_active: true }, { id: 2, name: 'Orion Floor', floor_number: 2, is_active: true }];
const desks: Space[] = Array.from({ length: 20 }, (_, index) => ({ id: index + 1, floor_id: index < 12 ? 1 : 2, name: `Desk A${String(index + 1).padStart(2, '0')}`, type: 'hot_desk', zone: ['North Zone', 'East Zone', 'South Zone', 'West Zone'][index % 4], x_coordinate: 80 + (index % 5) * 130, y_coordinate: 90 + Math.floor(index / 5) * 95, capacity: 1, is_active: true, description: 'Ergonomic desk with focused lighting and premium accessories.', equipment: [demoEquipment[index % demoEquipment.length], demoEquipment[(index + 1) % demoEquipment.length]] }));
const rooms: Space[] = Array.from({ length: 5 }, (_, index) => ({ id: 21 + index, floor_id: index % 2 === 0 ? 1 : 2, name: `Room ${String.fromCharCode(65 + index)}`, type: 'meeting_room', zone: ['North Zone', 'East Zone', 'South Zone', 'West Zone'][(index + 1) % 4], x_coordinate: 150 + index * 120, y_coordinate: 420 + (index % 2) * 120, capacity: [4, 6, 8, 10, 12][index], is_active: true, description: 'Premium meeting room with collaboration-ready AV.', equipment: [demoEquipment[3], demoEquipment[4], demoEquipment[5]] }));
export const demoSpaces: Space[] = [...desks, ...rooms];
